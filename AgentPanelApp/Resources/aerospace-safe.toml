# Managed by AgentPanel - do not edit manually
# ap-config-version: 3
# Purpose: AeroSpace sits idle until AgentPanel explicitly controls it.
# All windows are floating; tiling is disabled. AgentPanel handles
# workspace-scoped window cycling natively via Option-Tab / Option-Shift-Tab.

config-version = 2
start-at-login = false

[mode.main.binding]
# >>> user-keybindings
# Add your custom keybindings below. AgentPanel preserves this section across updates.
# <<< user-keybindings

[[on-window-detected]]
check-further-callbacks = true
run = 'layout floating'

# >>> user-config
# Add your custom AeroSpace config below. AgentPanel preserves this section across updates.
# <<< user-config
